@@ -964,13 +959,8 @@
     }
 
     if (channels == 2) {
-        if (post_type == 1) {
-            min_val_R = -(1 << (bit_depth - 1));
-            max_val_R = (1 << (bit_depth - 1)) - 1;
-        } else {
-            min_val_R = sign_extend(rc->read_uniform_bits(bit_depth), bit_depth);
-            max_val_R = sign_extend(rc->read_uniform_bits(bit_depth), bit_depth);
-        }
+        min_val_R = sign_extend(rc->read_uniform_bits(bit_depth), bit_depth);
+        max_val_R = sign_extend(rc->read_uniform_bits(bit_depth), bit_depth);
         
         b = rc->read_uniform_bits(1);
         if (b == 0) {
