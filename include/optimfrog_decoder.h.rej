@@ -51,6 +51,7 @@
     uint32_t bit_pos;
     uint32_t capacity;
     uint32_t flags;
+    uint32_t total_bits_read = 0;
 
     OFR_BitStream() : buffer(nullptr), bit_size(0), bit_pos(0), capacity(0), flags(0) {}
 
@@ -82,6 +83,7 @@
         }
 
         this->bit_pos += num_bits;
+        this->total_bits_read += num_bits;
         return value;
     }
 
